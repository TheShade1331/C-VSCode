#include<stdio.h>
int main()
{
    // char S[100];
    // printf("Enter your Name:");
    // scanf("%s", S);                               //using scanf //only scans 1 word Strings
    // printf("Your Name is %s",S);
    // return 0;

    // char T[100];
    // printf("Enter your Name:");
    // gets(T);                               //using gets //scans multi-word Strings
    // printf("Your Name is %s",T);
    // return 0;

    char U[100];
    printf("Enter your Name:");
    gets(U);
    puts(U);                               //using puts //prints multi-word Strings
    // printf("Your Name is %s",U);
    return 0;

} 